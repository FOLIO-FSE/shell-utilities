okapi_url=$(cat okapi.url)
tenant=$(cat tenant)

okapi_token=$(curl -s -X POST -H "Content-type: application/json" -H "X-Okapi-Tenant: ${tenant}" -d @okapi-login.json "${okapi_url}/authn/login" |jq -r .okapiToken)

echo $okapi_token |tee okapi.token



#!/usr/bin/env bash
set -euo pipefail

command -v curl >/dev/null || { echo "❌ curl required"; exit 1; }
command -v jq >/dev/null || { echo "❌ jq required"; exit 1; }

QUERY="${1:-}"
MODE="${2:-}"
ARG="${3:-}"

if [[ -z "$QUERY" ]]; then
  echo "Usage:"
  echo "  schema-get <module>"
  echo "  schema-get <module> schema <name>"
  echo "  schema-get <module> example <name>"
  echo "  schema-get <module> example-min <name> (only required fields)"
  exit 1
fi

HEADERS=(-H "Accept: application/vnd.github+json" -H "User-Agent: schema-get")
[[ -n "${GITHUB_TOKEN:-}" ]] && HEADERS+=(-H "Authorization: Bearer $GITHUB_TOKEN")

# ----------------------------
# MODULE RESOLUTION
# ----------------------------
Q=$(echo "$QUERY" | tr '[:upper:]' '[:lower:]')
BASE="${Q#mod-}"

# ✅ normalize common singular inputs
case "$BASE" in
  order-storage) BASE="orders-storage" ;;
  order) BASE="orders" ;;
  inventory) BASE="inventory-storage" ;;
esac

CANDIDATES=(
  "mod-$BASE"
  "$BASE"
  "mod-${BASE%-storage}"
  "mod-${BASE%-storage}-storage"
)

MODULE=""

for m in "${CANDIDATES[@]}"; do
  RESP=$(curl -s "${HEADERS[@]}" "https://api.github.com/repos/folio-org/$m")
  if [[ "$(echo "$RESP" | jq -r '.id // empty')" != "" ]]; then
    MODULE="$m"
    break
  fi
done

[[ -z "$MODULE" ]] && { echo "❌ Module not found"; exit 1; }

echo "🔍 Module: $MODULE"

# ----------------------------
# BRANCH
# ----------------------------
BRANCH=$(curl -s "${HEADERS[@]}" \
  "https://api.github.com/repos/folio-org/$MODULE" \
  | jq -r '.default_branch // empty')

[[ -z "$BRANCH" ]] && BRANCH="master"

echo "🌿 Branch: $BRANCH"

# ----------------------------
# RAML FILES
# ----------------------------
RAML_FILES=$(curl -s "${HEADERS[@]}" \
  "https://api.github.com/repos/folio-org/$MODULE/contents/ramls" \
  | jq -r '.[]? | select(.name|endswith(".raml")) | .path')

# ----------------------------
# FIND SCHEMA (FINAL FIXED)
# ----------------------------
find_schema() {
  local name="$1"
  name="${name%.json}"

  local best_match=""
  local best_score=0

  for file in $RAML_FILES; do
    CONTENT=$(curl -s \
      "https://raw.githubusercontent.com/folio-org/$MODULE/$BRANCH/$file")

    SCHEMAS=$(echo "$CONTENT" | grep -Eo 'schemas/[A-Za-z0-9_./-]+\.json' || true)

    for s in $SCHEMAS; do
      fname=$(basename "$s")
      base="${fname%.json}"

      score=0

      # ✅ exact match
      [[ "$base" == "$name" ]] && score=200

      # ✅ direct path match
      [[ "$s" == *"$name.json" ]] && score=180

      # ✅ composite
      [[ "$base" == "composite_$name" ]] && score=150

      # ✅ prefix match
      [[ "$base" == "$name"* ]] && score=100

      # ✅ contains fallback
      [[ "$base" == *"$name"* ]] && score=10

      # ❗ penalize collections heavily
      if [[ "$base" == *_collection ]]; then
        score=$((score - 150))
      fi

      # ✅ prefer shorter names
      score=$((score - ${#base} / 5))

      if (( score > best_score )); then
        best_score=$score
        best_match="$s"
      fi
    done
  done

  if [[ -n "$best_match" ]]; then
    echo "$best_match|$(basename "$best_match")"
  else
    echo "|"
  fi
}

# ----------------------------
# FETCH SCHEMA
# ----------------------------
fetch_schema() {
  local path="$1"
  local file="$2"
  local tmp="$3"

  # ✅ Try current module first
  URL="https://raw.githubusercontent.com/folio-org/$MODULE/$BRANCH/ramls/$path"
  HTTP=$(curl -s -w "%{http_code}" -o "$tmp" "$URL")

  if [[ "$HTTP" == "200" ]]; then
    echo "$URL"
    return
  fi

  # ✅ Try storage module automatically
  STORAGE_MODULE="${MODULE}-storage"

  URL2="https://raw.githubusercontent.com/folio-org/$STORAGE_MODULE/$BRANCH/ramls/$path"
  HTTP=$(curl -s -w "%{http_code}" -o "$tmp" "$URL2")

  if [[ "$HTTP" == "200" ]]; then
    echo "$URL2"
    return
  fi

  # ✅ Try acq-models fallback
  URL3="https://raw.githubusercontent.com/folio-org/acq-models/master/$MODULE/schemas/$file"
  HTTP=$(curl -s -w "%{http_code}" -o "$tmp" "$URL3")

  if [[ "$HTTP" == "200" ]]; then
    echo "$URL3"
    return
  fi

  # ✅ Try acq-models storage fallback
  URL4="https://raw.githubusercontent.com/folio-org/acq-models/master/$STORAGE_MODULE/schemas/$file"
  HTTP=$(curl -s -w "%{http_code}" -o "$tmp" "$URL4")

  if [[ "$HTTP" == "200" ]]; then
    echo "$URL4"
    return
  fi

  echo ""
}

# ============================
# SCHEMA MODE
# ============================
# ============================
# ✅ SCHEMA MODE (LIST + FETCH)
# ============================
if [[ "${MODE:-}" == "schema" ]]; then

  # ----------------------------
  # ✅ LIST SCHEMAS
  # ----------------------------
  if [[ -z "${ARG:-}" ]]; then
    echo
    echo "📦 Available schemas:"
    echo

    for file in $RAML_FILES; do
      CONTENT=$(curl -s \
        "https://raw.githubusercontent.com/folio-org/$MODULE/$BRANCH/$file")

      echo "$CONTENT" \
        | grep -Eo 'schemas/[A-Za-z0-9_./-]+\.json' \
        | sed -E 's#.*/##; s#\.json##'
    done | sort -u

    echo
    echo "💡 Usage:"
    echo "   schema-get $QUERY schema <name>"
    echo
    exit 0
  fi

  # ----------------------------
  # ✅ FETCH SINGLE SCHEMA
  # ----------------------------
  IFS="|" read SC_PATH SC_FILE <<< "$(find_schema "$ARG")"

  if [[ -z "$SC_FILE" ]]; then
    echo "❌ schema not found"
    exit 1
  fi

  TMP=$(/usr/bin/mktemp)
  URL=$(fetch_schema "$SC_PATH" "$SC_FILE" "$TMP")

  if [[ -z "$URL" ]]; then
    echo "❌ fetch failed"
    rm -f "$TMP"
    exit 1
  fi

  echo "📦 Schema: $SC_FILE"
  echo "🔗 URL: $URL"
  echo

  jq . "$TMP"

  rm -f "$TMP"
  exit 0
fi

# ============================
# EXAMPLE MODE
# ============================
if [[ "$MODE" == example* ]]; then
  [[ -z "$ARG" ]] && { echo "❌ provide schema name"; exit 1; }

  FULL=true
  [[ "$MODE" == "example-min" ]] && FULL=false

  IFS="|" read SC_PATH SC_FILE <<< "$(find_schema "$ARG")"
  [[ -z "$SC_FILE" ]] && { echo "❌ schema not found"; exit 1; }

  TMP=$(/usr/bin/mktemp)
  URL=$(fetch_schema "$SC_PATH" "$SC_FILE" "$TMP")
  [[ -z "$URL" ]] && { echo "❌ fetch failed"; exit 1; }

  echo "🧪 $( $FULL && echo Full || echo Minimal ) payload for: $SC_FILE"
  echo

  jq --argjson full "$FULL" '

  def uuid: "00000000-0000-0000-0000-000000000000";

  def gen($full; $k):

    if has("items") or (.type? == "array") then []
    elif has("enum") then .enum
    elif (.type? == "boolean") then false
    elif (.type? == "integer") then 0
    elif (.type? == "number") then 0

    elif has("properties") then
      (.properties // {}) as $props
      | (.required // []) as $req
      | if $full then
          reduce ($props | keys_unsorted[]) as $p (
            {};
            . + { ($p): ($props[$p] | gen($full; $p)) }
          )
        else
          reduce $req[] as $p (
            {};
            . + { ($p): ($props[$p] | gen($full; $p)) }
          )
        end

    else
      if ($k|test("id$|Id$")) then uuid else "string" end
    end;

  gen($full; "")
  ' "$TMP"

  rm -f "$TMP"
  exit 0
fi

# ============================
# ENDPOINTS
# ============================
echo
echo "📡 Endpoints:"

for file in $RAML_FILES; do
  curl -s \
    "https://raw.githubusercontent.com/folio-org/$MODULE/$BRANCH/$file" \
  | awk '
    /^[\/]/ { path=$1 }
    /^[ ]+(get|post|put|delete|patch):/ {
      method=toupper($1)
      gsub(":", "", method)
      if (path != "") print method, path
    }
  '
done | sort -u

echo
